{
  "title": "Assigning to console global fails at runtime",
  "stage": "run",
  "expectedDiagnosticContains": "Cannot access 'console' before initialization",
  "notes": "The compiler rewrites the assignment into a local let that is accessed in its TDZ."
}
