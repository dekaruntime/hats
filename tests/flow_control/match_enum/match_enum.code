enum Status {
  Loading,
  Ready,
  Failed,
}

const current = Status.Ready
const label = match (current) {
  Status.Loading => "loading",
  Status.Ready => "ready",
  Status.Failed => "failed",
  _ => "unknown",
}

console.log(label)
