{
  "title": "For-of with var binding fails",
  "stage": "parse",
  "expectedDiagnosticContains": "DekaScript for-of requires `const` or `let`",
  "notes": "DekaScript for-of loops require `const` or `let`; `var` is rejected."
}
