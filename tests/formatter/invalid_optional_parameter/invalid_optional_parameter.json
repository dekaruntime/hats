{
  "title": "Optional parameter syntax is rejected",
  "stage": "parse",
  "notes": "DekaScript parameters do not support the optional ? marker; this is a parse error.",
  "expectedDiagnosticContains": "DekaScript parameters require a type annotation"
}
